# Laravel Lesson レビュー①

## Todo一覧機能
・一覧画面のURIの設定
・一覧画面を表示
・DBに登録されているデータを取得する
・取得したデータを画面に渡す
・画面上にデータを表示する

### Todoモデルのallメソッドで実行しているSQLは何か
 SELECT * FROM `todos`;

### Todoモデルのallメソッドの返り値は何か
Collectionクラス

### 配列の代わりにCollectionクラスを使用するメリットは
配列としての使い方はそのまま通用し、その上で追加機能が使える

### view関数の第1・第2引数の指定と何をしているか
画面に表示したいbladeファイルを第一引数で指定し、第二引数に渡したいデータを連想配列の形で渡しています。
view('todo.index', ['todos' => $todos]);

### index.blade.phpの$todos・$todoに代入されているものは何か
Collectionインスタンスが代入されています。

## Todo作成機能
・一覧画面のURIの設定
・一覧画面を表示
・DBに登録されているデータを取得する
・取得したデータを画面に渡す
・画面上にデータを表示する

### Requestクラスのallメソッドは何をしているか
フォームから送信された入力値を全部、連想配列で一括取得しています。

### fillメソッドは何をしているか
  ▎ 連想配列で取得した値を->fill()を使用して、Todoインスタンスの各プロパティに一
  ▎ 括で代入しています。

### $fillableは何のために設定しているか
便利な一括代入には実は脆弱性があり、対策をせずに使用すると悪意のあるユーザに攻撃されてしまう恐れがあるため

### saveメソッドで実行しているSQLは何か
INSERT 文です。

### redirect()->route()は何をしているか
一覧画面にリダイレクトする処理

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
・SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要
・マイグレーションファイルをGitで共有することで、開発者全員が同じテーブルを作成することができる
・現在のデータベースの状態を他の開発者に共有することができる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up() php artisan migrate
down() php artisan migrate:rollback

### Seederクラスの役割は何か
レコードの作成を担う役割

### route関数の引数・返り値・使用するメリット
可読性が向上／URL変更時も web.php だけで済み保守性が向上

### @extends・@section・@yieldの関係性とbladeを分割するメリット
共通の部分を別ファイルにまとめ、重複するコードを共通化して再利用できるようになるため

### @csrfは何のための記述か
CSRFの攻撃手法である不特定多数の人に対して意図しないリクエスト送信をさせる攻撃の対策をするため

### {{ }}とは何の省略系か
Blade内のURLの記述の省略系